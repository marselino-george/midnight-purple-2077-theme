1.1.9
- Expands the VS2026 Lab shell layer to the full 94-token `Shell` and 28-token `ShellInternal` palette.
- Adds purple base, surface, control, subtle hover, toolbar/menu, tab, status bar, and shell header colors.
- Keeps the corrected binary `.pkgdef` length headers validated by `scripts/Update-VS2026ShellLayer.ps1`.
- Keeps the separate lab VSIX identity so testing does not replace the public Marketplace package.

Credits
- Original VS Code theme credit belongs to cyber samurai.
