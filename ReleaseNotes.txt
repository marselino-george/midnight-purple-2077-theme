1.1.8
- Experimental side-by-side VS2026 Lab build; this VSIX uses a separate identity and theme GUID from the public Marketplace package.
- Adds a Visual Studio 2026 Fluent `Shell` and `ShellInternal` layer using the same 7+10 token shape as the built-in tinted themes.
- Fixes the generated binary `.pkgdef` length headers so `DeclaredBytes` matches the actual category byte count.
- Keeps the Visual Studio 2022-compatible legacy theme categories for comparison while testing the VS2026 chrome colors.

Credits
- Original VS Code theme credit belongs to cyber samurai.
